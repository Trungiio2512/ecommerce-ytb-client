import React from "react";
import PropTypes from "prop-types";

const Sidebar = (props) => {
  return (
    <div className="w-full border-1 border-main border-solid rounded-sm">
      <aside>Sidebar</aside>
    </div>
  );
};

Sidebar.propTypes = {};

export default Sidebar;
